package vn.t3nexus.lib.common.domain.model;

/**
 * Represents a domain object that is defined by its attributes rather than a unique identity.
 * <br>Value objects are immutable by design and equality is based on their state.
 */
public interface ValueObject extends DomainObject {
}
